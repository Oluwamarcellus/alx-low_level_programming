#include<stdio.h>
/**
 * main - function that prints "with proper grammar...,"
 * Return: Returns 0
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
